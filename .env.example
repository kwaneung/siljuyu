# Required only for live fuel-price data. Keep server-only; never prefix with NEXT_PUBLIC.
OPINET_API_KEY=

# Optional. Enables address search in manual location UI; lat/lng fallback works without it.
KAKAO_REST_API_KEY=
